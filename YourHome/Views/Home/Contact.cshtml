
<h4> اكتب الرسالة او الشكوى الخاصة بك وسيتم التواصل معك على البريد الالكترونى..</h4>

@model Contactu
<div class="container">

    <div class="row">
        <div class="col-md-8">
            <section id="loginForm">
                @using (Html.BeginForm("ContactPost", "Home", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
                {
                    @Html.AntiForgeryToken()
                    <hr />
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    <div class="form-group">
                        <label class="col-md-2 control-label">الايميل</label>
                        <div class="col-md-10">
                            @Html.TextBoxFor(m => m.Mail, new { @class = "form-control" })
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-md-2 control-label">العنوان</label>
                        <div class="col-md-10">
                            <input type="text" class="form-control" />
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-md-2 control-label">الرسالة</label>
                        <div class="col-md-10">
                            @Html.TextAreaFor(m => m.MessageContent, new { @class = "form-control" })
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="ارسال" class="btn btn-default" />
                        </div>
                    </div>

                }
            </section>

        </div>
        <div class="col-md-4">
            <br />
            <br />
            <section id="socialLoginForm">
                <address>
                    Redmond, WA 98052-6399<br />
                    <abbr title="Phone">P:</abbr>
                    425.555.0100
                </address>

                <address>
                    <strong>Support:</strong>   <a href="mailto:Support@example.com">DaleelElmafkodinSupport@example.com</a><br />
                    <strong>Marketing:</strong> <a href="mailto:Marketing@example.com">DaleelElmafkodinMarketing@example.com</a>
                </address>
            </section>
        </div>
    </div>
</div>